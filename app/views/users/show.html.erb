<div class="individual-user-controller">  
  <%= render "shared/user_data", user: @user %>
  <div class="user-bio">
    <p>Bio</p>
    <p><%= @user.bio %></p>
  </div>
  <ul>
    <%= @user.posts.each do |post| %>
      <div class="post">
        <%= render "shared/post", post: post %>         
      </div>
    <% end %>
  </ul>
  <%= button_to "See all posts", user_posts_path(@user, @post) %>  
</div>

